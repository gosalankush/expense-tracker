# CLI Expense Tracker

A modular Command Line Interface (CLI) application built with Python to track personal expenses, categorize spending, and generate real-time financial summaries.

## Features

- **Add Expenses:** Record amounts, categories, and optional descriptions with automated timestamping.
- **Input Validation:** Enforces strict numeric validation and error handling for robust execution.
- **Data Persistence:** Automatically stores and reads expense records from a structured `expenses.json` file.
- **Category Analytics:** Computes total expenditures and dynamic percentage breakdowns across custom categories.

## Getting Started

### Prerequisites

- Python 3.10+

### Installation & Execution

1. Clone the repository:
   ```bash
   git clone https://github.com/gosalankush/expense-tracker.git
   cd expense-tracker